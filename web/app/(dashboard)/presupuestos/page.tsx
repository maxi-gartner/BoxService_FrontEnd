import { ModulePlaceholder } from "@/components/layout/ModulePlaceholder";

export default function PresupuestosPage() {
  return (
    <ModulePlaceholder
      icon="📋"
      title="Presupuestos"
      description="El backend de Presupuestos ya está migrado y se usa desde Taller (aprobar, rechazar, generar service). Falta armar la pantalla propia de gestión."
    />
  );
}
