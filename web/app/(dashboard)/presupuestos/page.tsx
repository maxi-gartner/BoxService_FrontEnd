import { ModulePlaceholder } from "@/components/layout/ModulePlaceholder";

export default function PresupuestosPage() {
  return (
    <ModulePlaceholder
      icon="📋"
      title="Presupuestos"
      description="La gestión de presupuestos (aprobar, rechazar, generar service) todavía corre sobre el mock. Se migra al backend nuevo con el mismo patrón que Clientes."
    />
  );
}
